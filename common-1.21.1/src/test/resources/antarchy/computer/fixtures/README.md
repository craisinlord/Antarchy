# Computer contract fixtures

These fixtures describe expected computer state transitions for validation tooling. They are not runtime datapack content and are not loaded by the game.

The repository currently has no computer-specific test harness. The fixtures cover first placement, duplicate disk consumption, protection of the built-in introduction module, authenticated ejection through Settings or Trash, rejection without authentication, and physical disk drops when the block is broken.
